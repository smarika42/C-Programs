// TO find the first and last digits of a number.
#include<stdio.h>
void main(){
	int a, i;
	printf("Enter a number: %d", a);
	scanf("%d", &a);
	for (a; a!=0; a/=10){
	int lastDigit=a%10	
	int firstDigit=
	}
}
